from .base import *

DEBUG = True

#configs for postgres
"""
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'DATABASE',
        'USER': 'YOURUSER',
        'PASSWORD': 'YOURPASSWORD',
        'HOST':  "localhost",
        'PORT': 5432, #chage it if you need it
    }
}
"""


#configs for sqlite
"""
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': 'temii.sqlite',
    }
}
"""